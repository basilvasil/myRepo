/*package com.revature.Read;
import com.revature.util.ConnectionFactory;
import org.apache.log4j.Logger;
import java.sql.PreparedStatement;
import java.sql.SQLException;
import java.util.ArrayList;
*/
public class Reader {
    /*Logger logger = Logger.getLogger(Reader.class);

    public static void readTable(String tableName){
        int i = 1;
        String read = "select * from "+tableName;
        ArrayList<String> table = new ArrayList<>();
        try {
            PreparedStatement ps = ConnectionFactory.getConnection().prepareStatement(read);
            ps.execute();
        } catch (SQLException e) {
            e.printStackTrace();
        }

    }

    public static void readColumn(){}

    public static void readRecord(){}*/
}
