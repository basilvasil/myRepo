package revature;

public class AccountLogIn {
    /*
        Scanner in = new Scanner (System.in);
        System.out.println("Enter your email address");
        string emailAddress = in.nextLine();
        System.out.println("Enter your password");
        sting password = in.nextLine()

        pseudo:
        System searches database
            if emailAddress is found{
                System.out(Please enter your password)
                    if password = database's password{
                        Display account info and balance

                        System.out(Press 1 to make a withdrawal, press 2 to make a deposit, press 3 to return to
                        the main menu, or press any other key to exit.)

                            if 1
                                System.out(Enter withdrawal amount)
                                    if balance < withdrawal
                                        System.out(Overdraft error)
                                    else
                                        balance = balance - withdrawal
                            else if 2
                                System.out(Enter deposit amount)
                                balance = balance + deposit
                            else if 3
                                Driver.mainMenu
                            else
                                System.out(Goodbye)
                    else
                        System.out(Password incorrect. Press 1 to try again, 2 to return to the main menu or
                        3 to exit)
                            if

     */

}
